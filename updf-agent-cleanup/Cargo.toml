[package]
name = "updf-agent-cleanup"
version = "0.1.0"
edition = "2024"

[dependencies]
# This crate drives the `agent-text-cleanup` CLI as an external binary (subprocess),
# so it needs nothing beyond std.
