[package]
name = "updf-api"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.9"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "process", "net"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
